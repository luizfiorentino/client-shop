export const selectallProducts = (state) => state.products.allProducts;

export const selectProductDetails = (state) => state.products.productDetails;

export const selectAllReviews = (state) => state.products.allReviews;
